<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lemon.dao.ShiroUseMapper">
    <!-- 登录 -->
    <select id="login" resultType="com.lemon.bean.TUser">
        SELECT
        id,username,password FROM t_user WHERE  username = #{username}
    </select>
</mapper>