<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lemon.dao.UserDao">
    <!-- 登录 -->
    <select id="findOneByName" resultType="UserBean">
        SELECT
        id,username,password FROM t_user WHERE  username = #{username}
    </select>
    <!-- 解锁用户 -->
    <update id="update">
        UPDATE t_user SET state = #{1} WHEN username = #{0}
    </update>
</mapper>